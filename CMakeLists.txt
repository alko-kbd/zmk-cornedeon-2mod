# if(TARGET app)
#     # Append battery logger ZMK
#     target_sources(app PRIVATE ${CMAKE_CURRENT_LIST_DIR}/src/battery_logger.c)
#     target_include_directories(app PRIVATE ${CMAKE_CURRENT_LIST_DIR}/src)
#     message(STATUS "Battery logger: src/battery_logger.c appended")
# endif()
